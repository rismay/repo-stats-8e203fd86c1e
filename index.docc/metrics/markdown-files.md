# Markdown file trend

@Metadata {
  @PageKind(article)
  @TitleHeading("Markdown file trend")
}

This view tracks cumulative `.md` file count across Month windows.

Latest cumulative `.md` files: 3189

@Image(source: "index-daily-markdown-file-chart-diagram", alt: "`.md` file trend")
